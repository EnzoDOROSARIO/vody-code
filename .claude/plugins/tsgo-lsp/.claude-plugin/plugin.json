{
  "name": "tsgo-lsp",
  "version": "1.0.0",
  "description": "TypeScript 7 native language server for this repo. TypeScript 7 ships no tsserver.js, so the official typescript-lsp plugin cannot start here; the native binary serves LSP itself, already patched with the Effect language service.",
  "lspServers": {
    "tsgo": {
      "command": "${CLAUDE_PROJECT_DIR}/node_modules/.bin/tsc",
      "args": ["--lsp", "--stdio"],
      "extensionToLanguage": {
        ".ts": "typescript",
        ".tsx": "typescriptreact",
        ".mts": "typescript",
        ".cts": "typescript",
        ".js": "javascript",
        ".jsx": "javascriptreact",
        ".mjs": "javascript",
        ".cjs": "javascript"
      }
    }
  }
}
